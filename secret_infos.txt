benna store supabase password = b1e2n3n4a5s6t7o8r9e0

SQL query suapbase to protect tables >
-- 1. تفعيل حماية RLS على جدول الأطباق
ALTER TABLE dishes ENABLE ROW LEVEL SECURITY;

-- 2. السماح للجميع بقراءة الأطباق
CREATE POLICY "Allow public read access" 
ON dishes FOR SELECT 
USING (true);

-- 3. السماح فقط لمول المحل بالإضافة والتعديل والحذف
CREATE POLICY "Allow admin full control" 
ON dishes FOR ALL 
TO authenticated 
USING (auth.jwt() ->> 'email' = 'e.youssef.code@gmail.com')
WITH CHECK (auth.jwt() ->> 'email' = 'e.youssef.code@gmail.com');

